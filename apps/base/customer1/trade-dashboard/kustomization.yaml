apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  # - tsproxy.yaml  # TODO: enable after Tailscale operator is installed
